import React from "react";
import "./PreferencesComponents.css";

const DietaryRestrictionsSearchBar = () => {
    return (
        <div className="dietary-restrictions">
            // search bar, with boxes indicating selected restrictions (cf. https://dev.to/salehmubashar/search-bar-in-react-js-545l)
            // option to click on selected restrictions to remove them
            // need to implement the data structure for storing dietary restriction options
        </div>
    );
}

const MealSizeSelection = () => {
    return (
        <div className="meal-size-selection">
            // select one among meal size options
        </div>
    );
}